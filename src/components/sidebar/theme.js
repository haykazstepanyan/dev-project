import { createTheme } from "@mui/material/styles";
import { colors } from "../../constants/constants";

export const filterRangeTheme = createTheme({
  palette: {
    primary: {
      main: colors.green,
    },
  },
});
